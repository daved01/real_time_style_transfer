

def log():
    """
    

    - Calculate average loss per epoch
    - Get timestamp from when the epoch was finished
    - Save average loss to file. If file does not exist, create it.
      If file exists, append.

    <model_name>_<dataset_name>_<style_image>_batchsize<batch_size>.log
    """



def plot_results():
    """
    Plots the loss over the epochs.

    Name: 
    loss_<model_name>_<dataset_name>_<style_image>_batchsize<batch_size>_epochs<num_epochs>.png
    """